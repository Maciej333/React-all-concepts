import CustomButton from '../CustomButton'

export default function ErrorButton() {
    return (
        <CustomButton
            data="Error button"
            buttonText="Error"
            bgColor="red"
        />
    )
}
